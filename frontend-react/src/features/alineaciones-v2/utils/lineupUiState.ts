/**
 * @deprecated Usar `../domain` o `lineupRuntimeState` directamente.
 * Re-export de compatibilidad; no añadir lógica aquí.
 */
export type { LineupBadgeTone, LineupUiState } from './lineupRuntimeState'
export { computeLineupUiState } from './lineupRuntimeState'
